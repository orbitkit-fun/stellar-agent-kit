{
  "name": "stellar-devkit",
  "version": "1.0.0",
  "private": true,
  "description": "Stellar DeFi devkit – Agent Kit SDK, x402 payments, CLI scaffolder, MCP",
  "workspaces": [
    "packages/stellar-agent-kit",
    "packages/x402-stellar-sdk",
    "packages/create-stellar-devkit-app",
    "packages/stellar-devkit-mcp",
    "sdk-fe",
    "ui"
  ],
  "scripts": {
    "build": "npm run build:root && npm run build --workspaces --if-present",
    "build:root": "tsc",
    "build:agent-kit": "npm run build -w packages/stellar-agent-kit",
    "build:x402": "npm run build -w packages/x402-stellar-sdk",
    "build:create-app": "npm run build -w packages/create-stellar-devkit-app",
    "build:mcp": "npm run build -w packages/stellar-devkit-mcp",
    "typecheck": "tsc --noEmit && npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "dev:sdk-fe": "npm run dev -w sdk-fe --if-present",
    "dev:ui": "npm run dev -w ui",
    "version:packages": "npm version patch -w stellar-agent-kit && npm version patch -w x402-stellar-sdk && npm version patch -w create-stellar-devkit-app && npm version patch -w stellar-devkit-mcp",
    "publish:packages": "npm run build:agent-kit && npm run build:x402 && npm run build:create-app && npm run build:mcp && npm publish -w stellar-agent-kit && npm publish -w x402-stellar-sdk && npm publish -w create-stellar-devkit-app && npm publish -w stellar-devkit-mcp"
  },
  "engines": {
    "node": ">=18"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "commander": "^12.0.0",
    "dotenv": "^16.0.0",
    "openai": "^4.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "zod": "^3.23.0"
  }
}
